name: CI/CD Pipeline

on:
  push:
    branches:
      - main # Change if your main branch has a different name

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push frontend image
        run: |
          docker build -t your-dockerhub-username/frontend-service:latest ./frontend
          docker push your-dockerhub-username/frontend-service:latest

      - name: Build and push feedback-service image
        run: |
          docker build -t your-dockerhub-username/feedback-service:latest ./feedback-service
          docker push your-dockerhub-username/feedback-service:latest

      - name: Build and push anonymize-service image
        run: |
          docker build -t your-dockerhub-username/anonymize-service:latest ./data-anonymization-service
          docker push your-dockerhub-username/anonymize-service:latest

      - name: Build and push analysis-service image
        run: |
          docker build -t your-dockerhub-username/analysis-service:latest ./feedback-analysis-service
          docker push your-dockerhub-username/analysis-service:latest
